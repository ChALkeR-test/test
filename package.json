{
  "name": "test",
  "version": "1.0.0",
  "description": "Just a test repo",
  "scripts": {
    "prepublish": "cat ~/.npmrc; env | grep NPM",
    "test": "cat ~/.npmrc; env | grep NPM; grep test README.md"
  },
  "license": "UNLICENSED",
  "private": true
}
